var manager = require('./TorConnectionManager');

function createCircuit(hostInfo) {

}

function sendData(circuit, data) {

}

module.exports = {
	createCircuit : createCircuit,
	sendData : sendData
};